# TEST_SPEC.md — Named Test-Case Catalog (Single Source of Truth)

> **Phase 2 deliverable.** Generated by Agent A (ARCHITECT) following the
> `derive_test_cases.md` skill protocol. Test names from P1 TEST_INVENTORY.yaml
> are preserved as-is where specified. This is the SINGLE SOURCE OF TRUTH for
> all test traceability checks at Gates 1-4 (v2.6 unified D4). Validated by
> Agent B (TECH_LEAD) before P2 exit. P3 Agent A implements tests FROM this
> catalog — not ad-hoc.

**Project**: `taskq-api`
**SRS version**: `01-requirements/SRS.md @ 2026-07-30 (v1.0.0)`
**SAD version**: `02-architecture/SAD.md @ Round 1 (P2)`
**TEST_INVENTORY.yaml version**: `TEST_INVENTORY.yaml @ Round 74 站5 (P1)`
**Generated**: 2026-09-05
**Active NFR Patterns**: NP-01, NP-02, NP-03, NP-04, NP-05, NP-06, NP-07, NP-08, NP-09, NP-10, NP-12, NP-13, NP-14, NP-15 (see §0b activation table for evidence)

---

## 0a. Test Naming Authority

P1 `TEST_INVENTORY.yaml` is the naming authority for canonical
integration-level FR test functions (`fr_tests.FR-XX.integration.*`).
Names preserved verbatim where the inventory names them; other entries are
derived via the 7-Question Protocol (Q1 happy path, Q2 validation, Q3
boundary, Q4 state transition, Q5 fault injection, Q6 NFR pattern, Q7
integration). Where the inventory name and Q1 collide, **inventory wins**.

---

## 0b. NFR Pattern Activation

The table enumerates every pattern (NP-01…NP-15) enumerated in
`derive_test_cases.md` Step 1. Activation is decidable from one of three
sources:

- `SRS: <keyword>` — Step 1 explicit trigger in §3/§4 of SRS.md
- `SAD: <module>` — Step 1b architecture-risk trigger
- `SEC: <threat-id>` — Step 1c SAD §6 STRIDE threat

| Pattern | Trigger (SRS keyword / SAD module / SEC threat) | Activated |
|---------|--------------------------------------------------|-----------|
| NP-01 (auth 401)          | SRS: "缺少或無效 → 401" (FR-03)                                  | ✅ |
| NP-02 (authz 403)         | SRS: "scope 不足 → 403" (FR-04)                                  | ✅ |
| NP-03 (rate limit 429)    | SRS: "超限 → 429" (FR-05)                                        | ✅ |
| NP-04 (validation 422)    | SRS: "驗證失敗 → 422" (FR-01, FR-10)                              | ✅ |
| NP-05 (idempotency)       | SRS: "name 唯一" 409 (FR-01/FR-05 row-lock + FR-02 run re-run)   | ✅ |
| NP-06 (latency SLA)       | SRS: "p95" (NFR-01) — Integration verifier in TEST_SPEC          | ✅ |
| NP-07 (dependency fault)  | SAD: SQLAlchemy `repository/session.py` (DB fail); Alembic migration fail | ✅ |
| NP-08 (security attack)   | SEC: T-01 SQL/string-concat injection (NFR-02); SEC: T-04 secret leakage (NFR-04) | ✅ |
| NP-09 (audit log)         | SRS: "correlation_id 同時出現於 header 與伺服器日誌" (FR-10 / NFR-04) | ✅ |
| NP-10 (data round-trip)   | SRS: "v3 資料搬遷可逆 — 樣本資料逐欄相同" (FR-07)                | ✅ |
| NP-11 (backward compat)   | not applicable to this round (greenfield API)                    | ❌ |
| NP-12 (pagination)        | SRS: "cursor-based 分頁" (FR-01)                                 | ✅ |
| NP-13 (concurrency)       | SAD: `service/rate_limit.py` row-locked bucket; `service/executor.py` `TaskGroup` + `MAX_CONCURRENT` cap | ✅ |
| NP-14 (encryption)        | SRS: "金鑰以 SHA-256 雜湊儲存" (FR-03 / NFR-02)                   | ✅ |
| NP-15 (timeout)           | SAD: `service/runner.py` `asyncio.create_subprocess_exec` + `wait_for` + `process.kill` (FR-02 / FR-08) | ✅ |

Activation evidence for each FR follows in §1. All Step 1b integration
variants (NP-07 / NP-13 / NP-15) live under `tests/integration/`.

---

## 1. Functional Requirement Test Cases

### FR-01: Task Resource CRUD API (POST/GET/GET-list/DELETE /v1/tasks)

**Classification**: `API_ENDPOINT`
**Active Patterns**: NP-04 (422), NP-05 (name uniqueness 409), NP-12 (cursor pagination)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_fr01_example_integration` | task_name="compile"; expected_status="201"; expected_id_present="true" | happy_path | Q1 (TC-FR01-01); AC-1.1 |
| 2 | `test_fr01_example_unit` | task_name=""; expected_status="422" | validation | Q2 (TC-FR01-02); AC-1.3 |
| 3 | `test_fr01_get_by_id_returns_full_record` | task_id="task-uuid-001"; expected_status="200"; expected_field_set="id,command,name,status,created_at" | happy_path | Q1; AC-1.5 |
| 4 | `test_fr01_get_unknown_id_returns_404` | task_id="task-uuid-missing"; expected_status="404"; expected_problem_type="/errors/not-found" | validation | Q2; AC-1.6 |
| 5 | `test_fr01_duplicate_name_returns_409` | first_task_name="compile"; second_task_name="compile"; expected_status="409" | state_transition | Q4; AC-1.4 |
| 6 | `test_fr01_list_pagination_cursor_consistent` | first_limit="50"; first_cursor=""; second_cursor="cursor-after-50"; expected_total_pages="2"; expected_status="200" | integration | Q7; AC-1.7 |
| 7 | `test_fr01_list_limit_above_max_returns_422` | limit="201"; expected_status="422"; expected_detail_field="limit" | boundary | Q3; AC-1.7 |
| 8 | `test_fr01_delete_requires_admin_scope` | actor_scope="write"; expected_status="403"; expected_existence_leak="false" | authz | NP-02; AC-1.9 |
| 9 | `test_fr01_delete_unknown_id_returns_404_for_missing` | actor_scope="admin"; task_id="task-uuid-missing"; expected_status="404"; expected_existence_leak="false" | validation | Q2; AC-1.8 (no-existence-leak half) |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR01-status-201 | `expected_status == "201"` | 1 |
| FR01-task-name-empty-422 | `len(task_name) == 0` | 2 |
| FR01-task-name-422-status | `expected_status == "422"` | 2 |
| FR01-get-id-200 | `expected_status == "200"` | 3 |
| FR01-get-field-set-contains-id | `"id,command,name,status,created_at".count(",") == 4` | 3 |
| FR01-get-unknown-404 | `expected_status == "404"` | 4 |
| FR01-get-unknown-problem-type | `expected_problem_type == "/errors/not-found"` | 4 |
| FR01-duplicate-name-409 | `first_task_name == second_task_name` | 5 |
| FR01-duplicate-name-conflict-status | `expected_status == "409"` | 5 |
| FR01-cursor-pages-eq-2 | `expected_total_pages == "2"` | 6 |
| FR01-limit-above-max-422 | `limit == "201"` | 7 |
| FR01-limit-above-max-status | `expected_status == "422"` | 7 |
| FR01-delete-non-admin-403 | `expected_status == "403"` | 8 |
| FR01-delete-non-admin-no-existence-leak | `expected_existence_leak == "false"` | 8 |
| FR01-delete-admin-unknown-404 | `expected_status == "404"` | 9 |
| FR01-delete-admin-no-existence-leak | `expected_existence_leak == "false"` | 9 |

> **Multi-scenario rule (v2.13.0)**: cases 8 and 9 each enumerate a
> distinct 403 vs 404 outcome on the SAME `DELETE` verb — kept separate
> rather than collapsed so the existence-leak invariant and the
> not-found outcome can both be machine-checked independently.

---

### FR-02: Task Execution Endpoint (POST /v1/tasks/{id}/run, GET /v1/tasks/{id}/runs, subprocess via shlex.split, state machine, persistence)

**Classification**: `API_ENDPOINT` + `STATE_MACHINE`
**Active Patterns**: NP-15 (subprocess timeout), NP-13 (race-free state transition)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_run_task_202_returns_run_id` | task_id="task-uuid-002"; command="echo hi"; expected_status="202"; expected_run_id_present="true" | happy_path | Q1; AC-2.1 |
| 2 | `test_no_shell_true_in_source_tree` | grep_pattern="shell=True"; expected_match_count="0" | static | NP-13/15 invariant (post-task #1); AC-2.2 |
| 3 | `test_runner_timeout_kills_subprocess_no_orphan` | task_id="task-uuid-003"; command="sleep 60"; timeout_sec="10"; expected_state="timeout"; expected_orphan_pgid_count="0" | fault_injection | Q5 (NP-15); AC-2.3 |
| 4 | `test_run_result_persists_to_task_results_table` | task_id="task-uuid-002"; expected_exit_code="0"; expected_stdout_tail_present="true"; expected_finished_at_present="true" | happy_path | Q1; AC-2.4 |
| 5 | `test_list_runs_ordered_newest_first` | task_id="task-uuid-002"; num_runs="3"; expected_first_run_index="2"; expected_last_run_index="0" | happy_path | Q1 (ordering); AC-2.5 |
| 6 | `test_task_state_machine_transitions` | task_id="task-uuid-002"; expected_transition_sequence="pending,running,done" | state_transition | Q4; AC-2.6 |

> **SAD architecture-risk trigger (Step 1b)**: `service/runner.py`
> spawns a subprocess, forcing NP-15. The shared async executor forces
> NP-13. Both cases (3 and 6) MUST live under
> `tests/integration/test_fr02.py`.

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR02-run-202-status | `expected_status == "202"` | 1 |
| FR02-run-202-run-id-present | `expected_run_id_present == "true"` | 1 |
| FR02-no-shell-true-zero-matches | `expected_match_count == "0"` | 2 |
| FR02-timeout-state-eq-timeout | `expected_state == "timeout"` | 3 |
| FR02-timeout-no-orphan-pgid | `expected_orphan_pgid_count == "0"` | 3 |
| FR02-result-persists-exit-code-0 | `expected_exit_code == "0"` | 4 |
| FR02-result-stdout-tail-present | `expected_stdout_tail_present == "true"` | 4 |
| FR02-result-finished-at-present | `expected_finished_at_present == "true"` | 4 |
| FR02-list-newest-first | `expected_first_run_index == "2"` | 5 |
| FR02-list-three-runs | `num_runs == "3"` | 5 |
| FR02-state-machine-three-transitions | `expected_transition_sequence.count(",") == 2` | 6 |

---

### FR-03: API Key Authentication (X-API-Key, SHA-256 hash, hmac.compare_digest, plaintext-once, revoked → 401, healthz/readyz exempt)

**Classification**: `SECURITY_CONTROL`
**Active Patterns**: NP-01 (401 missing/invalid key), NP-14 (hash storage)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_v1_endpoints_401_without_api_key` | header_x_api_key=""; expected_status="401"; expected_problem_type="/errors/unauthenticated" | validation | Q2 (NP-01); AC-3.1; AC-1.2 (401 on POST /v1/tasks) |
| 2 | `test_api_keys_table_stores_only_sha256_hashes` | sample_key_plaintext="tk-test-abcdefghijklmnop"; expected_hash_hex_length="64"; expected_plaintext_in_row="false" | happy_path | Q1 (NP-14); AC-3.2 |
| 3 | `test_key_compare_uses_constant_time` | comparison_target_hash="a"*64; expected_function_name_used="hmac.compare_digest" | security | NP-14; AC-3.3 |
| 4 | `test_key_create_prints_plaintext_once` | generated_key_prefix="tk-"; expected_stdout_contains_plaintext="true"; expected_db_persists_plaintext="false" | happy_path | Q1; AC-3.4 |
| 5 | `test_revoked_key_returns_401` | api_key_revoc_at_iso="2026-09-05T00:00:00Z"; expected_status="401" | validation | Q2; AC-3.5 |
| 6 | `test_healthz_readyz_no_auth_required` | endpoint="healthz"; header_x_api_key=""; expected_status="200" | happy_path | Q1 (exemption); AC-3.6 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR03-no-key-401-status | `expected_status == "401"` | 1 |
| FR03-no-key-problem-type | `expected_problem_type == "/errors/unauthenticated"` | 1 |
| FR03-hash-hex-len-64 | `expected_hash_hex_length == "64"` | 2 |
| FR03-no-plaintext-in-row | `expected_plaintext_in_row == "false"` | 2 |
| FR03-revoked-key-401 | `expected_status == "401"` | 5 |
| FR03-healthz-no-auth-200 | `expected_status == "200"` | 6 |

---

### FR-04: Scope Authorization (read<write<admin hierarchy, 403 no existence leak, single shared dependency)

**Classification**: `SECURITY_CONTROL`
**Active Patterns**: NP-02 (scope insufficient 403), NP-08 (no existence leak)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_scope_hierarchy_inclusion` | actor_scope="write"; required_scope="read"; expected_status="200"; lower_scope="read"; higher_scope="admin" | happy_path | Q1 (hierarchy inclusion); AC-4.1 |
| 2 | `test_insufficient_scope_403_no_existence_leak` | actor_scope="read"; target_task_id_exists="true"; expected_status="403"; expected_body_contains_task_id="false"; expected_problem_type="/errors/forbidden" | security | NP-02 + NP-08; AC-4.2; AC-N2.5 (cross-cut: 403 body no existence leak) |
| 3 | `test_all_v1_routes_share_single_auth_dependency` | imported_dependency_name="require_scope"; expected_import_count_v1_routes="1" | static | Q7; AC-4.3 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR04-hierarchy-write-covers-read | `lower_scope == "read" and higher_scope == "admin"` | 1 |
| FR04-insufficient-scope-403 | `expected_status == "403"` | 2 |
| FR04-insufficient-scope-no-exists-leak | `expected_body_contains_task_id == "false"` | 2 |
| FR04-insufficient-scope-problem-type | `expected_problem_type == "/errors/forbidden"` | 2 |
| FR04-single-dep-import | `expected_import_count_v1_routes == "1"` | 3 |

---

### FR-05: Rate Limiting (per-token token bucket, 429 + Retry-After, DB-persisted row-locked, /healthz /readyz exempt)

**Classification**: `STATE_MACHINE` + `API_ENDPOINT`
**Active Patterns**: NP-03 (429), NP-05 (idempotent cross-restart), NP-13 (row-lock concurrency)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_rate_limit_429_with_retry_after` | bucket_capacity="20"; refill_per_sec="5"; num_requests="21"; expected_status="429"; expected_retry_after_present="true" | boundary | Q3; AC-5.1 |
| 2 | `test_rate_bucket_persists_across_restart_and_uses_row_lock` | persisted_tokens_before="3"; persisted_tokens_after_one_call="2"; expected_lock_granularity="row" | state_transition | Q4 (NP-13); AC-5.2 |
| 3 | `test_healthz_readyz_exempt_from_rate_limit` | endpoint="readyz"; num_requests="100"; expected_status="200" | happy_path | Q1; AC-5.3 |

> **Multi-scenario rule (v2.13.0)** — case 1 enumerates three distinct
> facts (status=429, `Retry-After` header present, attempt count above
> capacity). All three are checked via separate sub-assertion rows so a
> future implementation can surface each signal independently.

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR05-burst-21-over-20-429 | `expected_status == "429"` | 1 |
| FR05-burst-21-retry-after-present | `expected_retry_after_present == "true"` | 1 |
| FR05-burst-21-over-capacity | `num_requests == "21"` | 1 |
| FR05-bucket-persists-decrement | `persisted_tokens_before == "3" and persisted_tokens_after_one_call == "2"` | 2 |
| FR05-bucket-row-lock-granularity | `expected_lock_granularity == "row"` | 2 |
| FR05-healthz-exempt-200 | `expected_status == "200"` | 3 |

---

### FR-06: Persistence + Transaction Boundaries (repository/ only, one Session/request, no string-concat SQL, eager-load, pool config)

**Classification**: `INFRASTRUCTURE`
**Active Patterns**: NP-04 (no SQL string concat), NP-06 (eager-load N+1 guard)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_repository_is_only_sqlalchemy_importer` | grep_pattern="import sqlalchemy"; expected_match_count_outside_repo="0" | static | Q7; AC-6.1 |
| 2 | `test_request_session_scope_commit_on_success_rollback_on_exception` | trigger_exception="true"; expected_committed_rows_on_exception="0"; expected_rolled_back_rows_on_exception="0" | state_transition | Q4; AC-6.2 |
| 3 | `test_no_sql_string_concatenation_in_source` | grep_pattern_concat_form="f\"SELECT \\|\\| \".*\\{.*\\}.*FROM"; expected_match_count="0" | static | NP-04; AC-6.3 |
| 4 | `test_list_endpoint_constant_sql_count_no_n_plus_1` | num_tasks_in_db="10"; expected_statement_count_per_request="4" | performance | NP-06 (N+1 guard); AC-6.4 |
| 5 | `test_engine_pool_size_and_pre_ping_configured` | expected_pool_size="5"; expected_pool_pre_ping="True" | configuration | Q1; AC-6.5 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR06-no-sqlalchemy-outside-repo | `expected_match_count_outside_repo == "0"` | 1 |
| FR06-rollback-zero-committed | `expected_committed_rows_on_exception == "0"` | 2 |
| FR06-rollback-confirmed | `expected_rolled_back_rows_on_exception == "0"` | 2 |
| FR06-no-sql-concat-zero | `expected_match_count == "0"` | 3 |
| FR06-list-constant-sql-count | `expected_statement_count_per_request == "4"` | 4 |
| FR06-pool-size-equals-5 | `expected_pool_size == "5"` | 5 |
| FR06-pool-pre-ping-true | `expected_pool_pre_ping == "True"` | 5 |

---

### FR-07: Alembic Three-step Migration (v1 tasks+api_keys, v2 tags + tasks.name unique, v3 result_json → task_results with data migration; downgrade preserved; round-trip bytewise)

**Classification**: `DATA_ENTITY` + `INTEGRATION`
**Active Patterns**: NP-10 (data round-trip), NP-07 (migration fault)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_alembic_upgrade_head_succeeds` | target_revision="head"; expected_alembic_exit_code="0" | happy_path | Q1; AC-7.1 |
| 2 | `test_alembic_downgrade_base_no_residual_tables` | target_revision="base"; expected_alembic_exit_code="0"; expected_residual_table_count="0" | happy_path | Q1; AC-7.2 |
| 3 | `test_alembic_v2_adds_tags_and_unique_index` | revision="v2"; added_table="task_tags"; expected_unique_index_name="ix_tasks_name_unique" | happy_path | Q1; AC-7.3 |
| 4 | `test_alembic_v3_splits_results_with_data_migration` | revision="v3"; sample_payload_exit_code="0"; sample_payload_stdout_tail="hello"; expected_table_after="task_results" | state_transition | Q4 (NP-10); AC-7.4 |
| 5 | `test_migration_round_trip_preserves_sample_data_bytewise` | sample_row_count="5"; round_trip_steps="upgrade_head;insert;downgrade_-1;upgrade_head"; expected_byte_identical_row_count="5" | data_round_trip | NP-10; AC-7.5; AC-N9.4 (cross-cut: real SQLite file round-trip) |
| 6 | `test_migration_files_have_real_downgrade_no_drop_table_shortcuts` | revision="v3"; forbidden_pattern="op.execute(\"DROP TABLE"; expected_match_count="0" | static | Q7; AC-7.6 |
| 7 | `test_migration_offline_sql_generation_matches_expectations` | alembic_mode="offline"; expected_sql_contains_create_tasks="true" | happy_path | Q1; AC-7.7 |

**Properties** — Direction B opt-in for FR-07. The v3 `result_json`
→ `task_results` move is a reversible algebraic split, so the
round-trip is a universal invariant.

| property_id | invariant (predicate over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR07-round-trip-bytewise | `result_after == sample_input` | 5 |

> The `result_after == sample_input` form degrades to `needs_review`
> because the LHS / RHS are symbolic — the actual property test (P4
> `hypothesis @given`) will substitute domain fixtures. P2 just locks
> the property *name* and the syntactic shape.

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR07-upgrade-head-exit-0 | `expected_alembic_exit_code == "0"` | 1 |
| FR07-downgrade-base-exit-0 | `expected_alembic_exit_code == "0"` | 2 |
| FR07-downgrade-no-residual | `expected_residual_table_count == "0"` | 2 |
| FR07-v2-unique-index-name | `expected_unique_index_name == "ix_tasks_name_unique"` | 3 |
| FR07-v3-table-after-move | `expected_table_after == "task_results"` | 4 |
| FR07-round-trip-byte-count | `expected_byte_identical_row_count == "5"` | 5 |
| FR07-round-trip-row-count-input | `sample_row_count == "5"` | 5 |
| FR07-no-drop-table-shortcut | `expected_match_count == "0"` | 6 |

---

### FR-08: Async Executor (asyncio.TaskGroup, TASKQ_DRAIN_TIMEOUT, TASKQ_MAX_CONCURRENT, wait_for + process.kill, CancelledError propagation)

**Classification**: `STATE_MACHINE`
**Active Patterns**: NP-15 (timeout kills child), NP-13 (max concurrent cap)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_runner_concurrency_capped_at_max_concurrent` | max_concurrent="8"; submitted_tasks="50"; expected_active_at_peak="8"; expected_queued_remainder="42" | state_transition | Q4 (NP-13); AC-8.1 |
| 2 | `test_shutdown_graceful_drain_marks_overdue_interrupted` | drain_timeout_sec="30"; in_flight_task_count="3"; expected_drain_exit_code="0"; expected_overdue_state_after_drain="interrupted" | integration | Q7; AC-8.2 |
| 3 | `test_timeout_kills_child_process_no_orphans` | command="sleep 60"; timeout_sec="5"; expected_orphan_ps_count="0" | fault_injection | Q5 (NP-15); AC-8.3 |
| 4 | `test_cancelled_error_propagates_through_exception_handlers` | trigger_cancel="true"; expected_cancellederror_caught_count="0"; expected_cancellederror_observed_in_caller="true" | validation | Q2 (NFR-03); AC-8.4 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR08-concurrency-peak-equals-max | `expected_active_at_peak == "8"` | 1 |
| FR08-concurrency-queued-remainder | `submitted_tasks == "50" and max_concurrent == "8" and expected_queued_remainder == "42"` | 1 |
| FR08-drain-exit-code-0 | `expected_drain_exit_code == "0"` | 2 |
| FR08-drain-overdue-state | `expected_overdue_state_after_drain == "interrupted"` | 2 |
| FR08-timeout-no-orphan-ps | `expected_orphan_ps_count == "0"` | 3 |
| FR08-cancel-propagates-not-caught | `expected_cancellederror_caught_count == "0"` | 4 |
| FR08-cancel-observed-in-caller | `expected_cancellederror_observed_in_caller == "true"` | 4 |

---

### FR-09: Health Checks + Observability (/healthz, /readyz, /v1/metrics)

**Classification**: `API_ENDPOINT`
**Active Patterns**: NP-07 (DB fail → 503), NP-09 (correlation id)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_healthz_200_no_auth` | endpoint="healthz"; expected_status="200"; expected_body_status_field="ok" | happy_path | Q1; AC-9.1 |
| 2 | `test_readyz_200_when_db_ok_and_migrations_at_head` | db_state="ok"; alembic_current="head"; expected_status="200" | happy_path | Q1; AC-9.2 |
| 3 | `test_readyz_503_when_db_unreachable` | db_state="unreachable"; expected_status="503"; expected_detail_contains_db="true" | fault_injection | Q5 (NP-07); AC-9.3 |
| 4 | `test_readyz_503_when_migration_not_at_head` | alembic_current="v1"; alembic_head="v3"; expected_status="503"; expected_detail_contains_migration="true" | fault_injection | Q5 (NP-07 / R9); AC-9.4 |
| 5 | `test_metrics_admin_scope_no_db_url_leak` | actor_scope="admin"; expected_status="200"; expected_response_contains_db_url_password="false" | security | NP-08 (NFR-04); AC-9.5; AC-N4.2 (cross-cut: DB URL password not in /v1/metrics response) |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR09-healthz-200 | `expected_status == "200"` | 1 |
| FR09-healthz-body-ok | `expected_body_status_field == "ok"` | 1 |
| FR09-readyz-200-db-ok | `expected_status == "200"` | 2 |
| FR09-readyz-503-db-unreachable | `expected_status == "503"` | 3 |
| FR09-readyz-503-detail-db | `expected_detail_contains_db == "true"` | 3 |
| FR09-readyz-503-migration-not-head | `expected_status == "503"` | 4 |
| FR09-readyz-503-detail-migration | `expected_detail_contains_migration == "true"` | 4 |
| FR09-metrics-admin-200 | `expected_status == "200"` | 5 |
| FR09-metrics-no-db-url-leak | `expected_response_contains_db_url_password == "false"` | 5 |

---

### FR-10: RFC 7807 Error Contract (Content-Type, 6-field body, no internals leak, correlation_id consistent, full status mapping)

**Classification**: `SECURITY_CONTROL` + `INTEGRATION`
**Active Patterns**: NP-04 (422), NP-08 (no internal leak), NP-09 (correlation id)

> **Multi-scenario rule (v2.13.0)** — §8 of SPEC.md enumerates 8 distinct
> error-code mappings (422 / 401 / 403 / 404 / 409 / 429 / 503 / 500).
> Each becomes its OWN case row below; one case row per scenario so the
> `Content-Type` invariant and the `detail`-no-leak invariant can be
> checked per status code.

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_all_error_responses_use_problem_json_content_type` | expected_content_type="application/problem+json" | static | Q7; AC-10.1 |
| 2 | `test_problem_json_body_has_required_fields` | expected_field_set="type,title,status,detail,instance,correlation_id"; expected_field_count="6" | happy_path | Q1; AC-10.2 |
| 3 | `test_500_detail_omits_internals` | trigger_internal_exception="sqlalchemy.exc.OperationalError"; expected_status="500"; expected_body_contains_stacktrace="false"; expected_body_contains_sql_statement="false"; expected_body_contains_file_path="false" | security | NP-08; AC-10.3; AC-N2.6 (cross-cut: 500 body no internals) |
| 4 | `test_correlation_id_consistent_across_response_and_log` | generated_correlation_id="abc-123-correlation"; expected_response_header_value="abc-123-correlation"; expected_log_line_contains_id="true" | happy_path | NP-09; AC-10.4 |
| 5 | `test_error_code_mapping_observed_per_spec_table` | status_422_input_name_oversize="name-oversized-payload"; status_422_expected="422"; status_401_no_auth_input=""; status_401_expected="401"; status_403_actor_scope="read"; status_403_expected="403"; status_404_unknown_id="task-uuid-missing"; status_404_expected="404"; status_409_duplicate="compile-conflict"; status_409_expected="409"; status_429_input_count="21"; status_429_expected="429"; status_503_db_state="unreachable"; status_503_expected="503"; status_500_internal="unhandled-exception"; status_500_expected="500" | validation | Q2 (table of 8); AC-10.5 |

> **Multi-scenario shape for case 5** — the single `Inputs` cell
> carries the eight distinct expected statuses. The sub-assertion table
> below splits them into ONE sub-assertion per status code so each is
> independently decidable (case-5 still references Inputs / its
> `expected_status_set` is one declaration; sub-assertions verify
> membership / mapping individually).

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| FR10-content-type-problem-json | `expected_content_type == "application/problem+json"` | 1 |
| FR10-required-field-count-6 | `expected_field_count == "6"` | 2 |
| FR10-500-status | `expected_status == "500"` | 3 |
| FR10-500-no-stacktrace-leak | `expected_body_contains_stacktrace == "false"` | 3 |
| FR10-500-no-sql-leak | `expected_body_contains_sql_statement == "false"` | 3 |
| FR10-500-no-filepath-leak | `expected_body_contains_file_path == "false"` | 3 |
| FR10-correlation-header-present | `expected_response_header_value == "abc-123-correlation"` | 4 |
| FR10-correlation-log-line-present | `expected_log_line_contains_id == "true"` | 4 |
| FR10-mapping-422 | `status_422_expected == "422"` | 5 |
| FR10-mapping-401 | `status_401_expected == "401"` | 5 |
| FR10-mapping-403 | `status_403_expected == "403"` | 5 |
| FR10-mapping-404 | `status_404_expected == "404"` | 5 |
| FR10-mapping-409 | `status_409_expected == "409"` | 5 |
| FR10-mapping-429 | `status_429_expected == "429"` | 5 |
| FR10-mapping-503 | `status_503_expected == "503"` | 5 |
| FR10-mapping-500 | `status_500_expected == "500"` | 5 |

> The eight mapping rows in case 5 carry paired `status_NNN_*` /
> `expected_NNN_*` keys, with the `status_NNN_*` value being the trigger
> input description and `expected_NNN_*` being the literal HTTP code
> from the spec's §7 status table. These two strings are equal by
> construction when the AC holds (e.g. a 422 trigger maps to 422) and
> the test asserts that equality.

---

## 2. Cross-Cutting Test Cases

Cross-cutting cases that span multiple FRs (none declared as
integration-test inventory entries beyond those in §1):

### NFR Integration (TEST_SPEC as verifier)

These NFRs are integration-level; the verifier is an integration test
executed by `tests/integration/`. Inputs and sub-assertions are the
machine-checkable declarations; P4 implements the test in Python.

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_deployment_example` | service_start_timeout_sec="10"; readyz_body_contains_db="true"; readyz_body_contains_alembic="true" | smoke | TC-N09-01 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| N09-readyz-body-has-db | `readyz_body_contains_db == "true"` | 1 |
| N09-readyz-body-has-alembic | `readyz_body_contains_alembic == "true"` | 1 |

### NFR Integration — Performance (NFR-01 / NP-06 verifier)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_get_single_task_p95_under_30ms` | p95_budget_ms="30"; rows="10000"; endpoint="GET /v1/tasks/{id}" | performance | NFR-01 AC-N1.1 |
| 2 | `test_list_tasks_p95_under_80ms` | p95_budget_ms="80"; rows="10000"; endpoint="GET /v1/tasks?limit=50" | performance | NFR-01 AC-N1.2 |
| 3 | `test_list_endpoint_constant_sql_count_no_n_plus_1` | expected_statement_count="4"; rows="10000" | performance | NFR-01 AC-N1.3 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| N01-get-single-p95-budget-30 | `p95_budget_ms == "30"` | 1 |
| N01-list-p95-budget-80 | `p95_budget_ms == "80"` | 2 |
| N01-list-constant-sql-count | `expected_statement_count == "4"` | 3 |
| N01-rows-fixture-10000 | `rows == "10000"` | 1, 2, 3 |

### NFR Integration — Concurrency / Dependency Fault (NP-13 / NP-07)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_rate_limit_under_concurrent_workers_no_oversend` | num_workers="20"; max_concurrent="8"; expected_oversend_count="0" | concurrency | NP-13 / FR-05 |
| 2 | `test_db_unreachable_causes_readyz_503_not_silent_retry` | db_state="unreachable"; observed_retry_attempts="3"; expected_final_response="503" | fault_injection | NP-07 / NFR-03; AC-N3.3 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| N13-rate-no-oversend | `expected_oversend_count == "0"` | 1 |
| N07-readyz-503-not-silent | `expected_final_response == "503"` | 2 |
| N07-retry-attempts-bounded | `observed_retry_attempts == "3"` | 2 |

### NFR Integration — Subprocess Timeout (NP-15)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_subprocess_timeout_no_orphan_after_service_restart` | command="sleep 60"; timeout_sec="5"; service_restart="true"; expected_orphan_pid_count="0" | fault_injection | NP-15 / FR-02+FR-08 |
| 2 | `test_cancelled_error_not_swallowed_in_concurrent_drain` | trigger_cancel_during_drain="true"; expected_swallowed_count="0" | validation | NP-13+NFR-03; AC-N3.2 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| N15-no-orphan-after-restart | `expected_orphan_pid_count == "0"` | 1 |
| N13-cancel-not-swallowed | `expected_swallowed_count == "0"` | 2 |

### NFR Integration — Coverage Gate (NFR-10)

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_integration_coverage_at_least_80_percent` | coverage_floor_percent="80"; target_path="03-development/tests/integration"; coverage_source="03-development/src" | static | NFR-10 AC-N10.1 |
| 2 | `test_integration_uses_asgi_transport_only` | required_transport="httpx.ASGITransport"; forbidden_import_count="0" | static | NFR-10 AC-N10.2 |
| 3 | `test_integration_covers_required_scenarios` | required_status_codes="401,403,404,409,422,429,503"; required_scenarios="crud_chain,migration_round_trip,rate_limit_trigger,rate_limit_recovery,graceful_drain"; missing_scenario_count="0" | validation | NFR-10 AC-N10.3 |

**Sub-assertions**

| rule_id | predicate (over Inputs / result) | applies_to (case #) |
|---|---|---|
| N10-integration-coverage-floor | `coverage_floor_percent == "80"` | 1 |
| N10-asgi-transport-only | `forbidden_import_count == "0"` | 2 |
| N10-scenario-checklist-complete | `missing_scenario_count == "0"` | 3 |

### Deployment Smoke

| # | Test Function | Inputs | Type | Derivation |
|---|---|---|---|---|
| 1 | `test_app_starts_and_health_endpoint_returns_200` | startup_grace_sec="3"; readyz_retry_budget_sec="5"; expected_readyz_status="200" | smoke | TC-N09-01; AC-N9.1 |

---

## 3. Deferred to Downstream Phases

> **NFR Layering Hard Rule**: every Unit/Static NFR test from
> `TEST_INVENTORY.yaml` MUST be declared here, with the AC's threshold
> carried as `key="value"` in the `Inputs` cell so the P4 implementation
> reads the number it asserts rather than retyping it. P3 implementation
> in `tests/static/` (and `tests/unit/`).

The TEST_INVENTORY.yaml inventory lists one unit/static NFR entry
(`TC-N02-01 test_security_example`); it appears below. Additional
static NFR tests (bandit coverage, CORS allowlist scan, docstring /
`[FR-XX]` tag coverage, `.importlinter` exit-0, dependency-tree license
scan, mutation-score aggregator, `radon mi` aggregate, `radon cc`
peak, file/dir/handler size gates, OpenAPI schema round-trip,
`make verify-system` orchestration) are anchored at the
**NFR-XX row level** in the table so P3 has a placeholder for every
NFR whose verifier is a static gate:

| # | NFR | Test Function | Layer | Inputs | Title |
|---|---|---|---|---|---|
| 1 | NFR-02 | `test_security_example` | static | shell_true_match_count="0"; eval_match_count="0"; exec_match_count="0"; sql_concat_match_count="0"; key_hash_hex_length="64"; bandit_high_count="0"; bandit_medium_count="0"; cors_default_deny="true" | Bandit + grep + SHA-256 + CORS gates — AC-N2.1, AC-N2.2, AC-N2.3, AC-N2.4 |
| 2 | NFR-05 | `test_docstring_coverage_with_fr_nfr_tags` | static | missing_docstring_count="0"; fr_tagged_def_count="100%"; nfr_tagged_def_count="100%" | Docstring 100% with `[FR-XX]`/`[NFR-XX]` tag — AC-N5.1 |
| 3 | NFR-06 | `test_importlinter_layers_contract_exits_0` | static | contract_topology="api>service>repository>models"; expected_exit_code="0"; sqlalchemy_outside_repository_count="0"; expected_blanket_ignore_imports_count="0" | `.importlinter` 4-layer + sqlalchemy forbidden + no-bypass — AC-N6.1, AC-N6.2, AC-N6.4 |
| 4 | NFR-06 | `test_importlinter_runtime_actually_blocks_sqlalchemy_in_service` | static | blocked_module="sqlalchemy"; inserted_import_target="service"; expected_lint_imports_exit_code="1" | import-linter block runtime guard — AC-N6.3 |
| 5 | NFR-07 | `test_pip_licenses_all_in_allowlist` | static | license_allowlist_size="5"; license_allowlist="MIT\\,BSD-2-Clause\\,BSD-3-Clause\\,Apache-2.0\\,PSF"; direct_count_plus_transitive_count_under_audit="100"; disallow_match_count="0"; requirements_lock_present="true" | Full dependency-tree license scan + lock file present — AC-N7.1, AC-N7.2, AC-N7.3 |
| 6 | NFR-08 | `test_mutation_score_at_least_70` | static | mutation_score_floor="70"; scope_pattern="service/+repository/"; mutation_testing_enabled="true" | Mutation score ≥ 70 (service/+repository/) with harness_config flag — AC-N8.1, AC-N8.2 |
| 7 | NFR-11 | `test_project_mi_at_least_80` | static | mi_floor="80"; cc_ceiling="10"; max_file_lines="400"; max_dir_files="15"; max_handler_lines="40" | Readability: MI ≥ 80, CC ≤ 10, file/dir/handler limits — AC-N11.1, AC-N11.2, AC-N11.3, AC-N11.4 |
| 8 | NFR-12 | `test_make_verify_system_chains_alembic_full_suite_smoke_round_trip` | integration | chain_steps="upgrade_head\\;full_test_suite\\;smoke\\;downgrade_base\\;upgrade_head"; expected_exit_code="0"; expected_stdout_marker="verify-system: PASS" | `make verify-system` end-to-end orchestration — AC-N12.1, AC-N12.2 |
| 9 | NFR-03 | `test_no_bare_except_or_swallow_exception` | static | bare_except_count="0"; except_exception_pass_count="0"; scan_path="03-development/src" | No bare `except:` / `except Exception: pass` in source — AC-N3.1 |
| 10 | NFR-03 | `test_failed_migration_rolls_back_to_prior_revision` | integration | trigger_migration_failure="true"; expected_revision_after="<prior_revision>"; expected_partial_table_count="0" | Failed migration rolls back transaction; DB stays at prior revision — AC-N3.4 |
| 11 | NFR-04 | `test_redact_sk_token_bearer_pg_url` | static | sample_sk_key="sk-abcdefghijklmnop"; sample_token_fragment="token=abc123"; sample_bearer_header="Bearer xyz789"; sample_pg_url="postgres://user:pass@host/db"; expected_redaction_marker="[REDACTED]"; expected_leak_count="0" | All four redaction patterns replaced with `[REDACTED]` in stdout_tail / stderr_tail / log — AC-N4.1 |
| 12 | NFR-05 | `test_openapi_schema_summary_description_present` | static | schema_path="/openapi.json"; v1_endpoint_count_min="1"; expected_summary_field="non-empty"; expected_description_field="non-empty" | Every `/v1/*` endpoint has non-empty `summary` + `description` in OpenAPI — AC-N5.2 |
| 13 | NFR-07 | `test_sbom_json_has_required_fields_per_dependency` | static | sbom_path="08-config/SBOM.json"; required_field_set="name,version,license,direct_or_transitive"; missing_field_count="0" | SBOM JSON has `name` / `version` / `license` / `direct\|transitive` per dependency — AC-N7.4 |
| 14 | NFR-08 | `test_crg_cohesion_healthy_not_lowered` | static | config_path=".methodology/harness_config.json"; framework_default_min="framework_value"; configured_value_min="framework_value" | `crg_cohesion_healthy` not lowered below framework default — AC-N8.3 |
| 15 | NFR-09 | `test_zero_assert_count_is_zero` | static | zero_assert_function_count="0"; scan_path="03-development/tests" | AST scan: zero test functions with zero asserts — AC-N9.2 |
| 16 | NFR-09 | `test_no_test_exclusion_via_config` | static | forbidden_keys="ignore,k,deselect,collect_ignore"; forbidden_testpath_removal="true"; violation_count="0" | No test exclusion via `--ignore` / `-k` / `--deselect` / `collect_ignore` / removed testpaths — AC-N9.3 |
| 17 | NFR-09 | `test_traceability_verified_only_after_test_passes` | static | traceability_path="TRACEABILITY_MATRIX.md"; verified_rows_with_unrun_test="0" | `TRACEABILITY_MATRIX.md` `VERIFIED` set only after test passes — AC-N9.5 |

> Layer per row is the same `unit`/`static`/`integration` value
> TEST_INVENTORY.yaml carries. Rows 7 and 8 are integration-level but
> have NO entry in `test_inventory.tests` (only the inventory entry
> TC-N02-01 has a layer unit/static); they appear here as anchor rows
> so the Deferred table is complete for every NFR whose verifier is a
> static gate or an end-to-end orchestration test. P3 may extend the
> inventory with `tc_id` rows for each later.

---

## 4. Summary

| Metric | Count |
|---|---|
| FRs covered | 10 (FR-01 … FR-10) |
| Total test cases (FR + cross-cutting) | 54 |
| Deferred-to-Downstream rows | 17 (1 inventory-anchored + 16 NFR-anchored static/integration gate rows) |
| By type: happy_path | 14 |
| By type: validation / failure | 11 |
| By type: boundary | 2 |
| By type: state_transition | 5 |
| By type: fault_injection | 5 |
| By type: static / configuration | 7 |
| By type: performance | 3 |
| By type: concurrency | 2 |
| By type: security / NP-08 | 4 |
| By type: smoke | 2 |
| Active NFR patterns applied | 13 (NP-01, NP-02, NP-03, NP-04, NP-05, NP-06, NP-07, NP-08, NP-09, NP-10, NP-12, NP-13, NP-14, NP-15) — 14 minus NP-11 (not applicable) |
| Properties declared (Direction B) | 1 (FR-07 round-trip bytewise) |

---

*Generated by: derive_test_cases.md v1.1 | harness-methodology v2.13.0*
