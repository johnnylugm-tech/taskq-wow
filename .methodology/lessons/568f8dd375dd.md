---
key: 568f8dd375dd
source: gate-block
phase: 3
dimension: 
fr_ids: FR-01
created_at: 2026-09-04
---

**Failure:** Gate 1 blocked [arch_contract_coverage]: An import-linter contract exists and does not reach every delivered module
**Fix:** The entry lists modules this project ships that appear in no contract's source modules. `lint-imports` reports those contracts kept regardless of what these modules import, so the boundary is claimed and not enforced — the composition root importing the database driver directly is the usual shape. Add the named modules to an existing contract's sources, or name a package above them (naming the root package covers everything under it), then re-run the gate. Deleting the contracts also clears this, and is the wrong fix: a project with no contract is not blocked here, but it has stopped claiming a boundary rather than started keeping one.
