# Specification Tracking Matrix — taskq-api

> On-demand Lazy Load template.

## Project Info
- Project Name: taskq-api
- Version: v1.0.0 (2026-07-30)
- Spec Source: SPEC.md (project root)
- SRS Source: 01-requirements/SRS.md (transcription of SPEC.md)
- Created: 2026-09-05

## Specification Status

> **The Status column is machine-refreshed** — `advance-phase` overwrites each
> FR's Status from `build_traceability`'s live code/test scan (IN_PROGRESS once
> code/module exists, VERIFIED once code+test exist). The authoritative status is
> that scan / `quality_manifest.json`, NOT this hand-filled cell. Fill the
> semantic columns (Spec Description / Intent Class / Decision Framework / Notes);
> leave Status to refresh itself (a hand-edit is overwritten on the next advance).

| FR ID | Spec Description | Intent Class | Decision Framework | Status | Notes |
|-------|-----------------|--------------|-------------------|--------|-------|
| FR-01 | Task resource CRUD API: POST/GET/GET-list/DELETE /v1/tasks with per-method scope (write/read/read/admin), pydantic validation, cursor-based pagination, problem+json error contract. | functional / behavioral | DERIVED from SPEC.md lines 83–91; chose 201/401/422/409/404/403 status mapping with no-existence-leak discipline for FR-04 cross-cut. | DRAFT | 9 ACs (AC-1.1..AC-1.9). ACs decided by integration tests via httpx.ASGITransport per SPEC §8 #4–8. Owner: 03-development. |
| FR-02 | Task execution endpoint: POST /v1/tasks/{id}/run returns 202 + run_id; runs via `asyncio.create_subprocess_exec(*shlex.split(command))` (shell=True forbidden); state machine pending→running→{done,failed,timeout}; results persisted to `task_results`; GET /v1/tasks/{id}/runs lists history newest-first. | functional / behavioral | DERIVED from SPEC.md lines 95–99; chose subprocess isolation via tmp_path, no-shell-true grep gate, kill()+await wait() no-orphan guard, state-machine transition test. | DRAFT | 6 ACs (AC-2.1..AC-2.6). Cross-cuts FR-08 (async runner) and NFR-02 (no shell=True). Owner: 03-development. |
| FR-03 | API Key authentication: every /v1/* requires X-API-Key (401 otherwise); keys stored as SHA-256 hashes (plaintext never persisted); comparison via `hmac.compare_digest` (constant-time); keys created via `python -m taskq_api key create --scope <scope>`, plaintext printed once; revoked keys (non-null `revoked_at`) rejected; /healthz and /readyz exempt. | functional / behavioral / security | DERIVED from SPEC.md lines 103–107; chose sha256-hex-64-char row check, constant-time compare call-graph assertion, plaintext-once stdout capture, revoked-key-still-401 invariant. | DRAFT | 6 ACs (AC-3.1..AC-3.6). Cross-cuts NFR-02 (security) and NFR-04 (key plaintext handling). Owner: 03-development. |
| FR-04 | Scope authorization: per-key scope `read` < `write` < `admin` (hierarchical inclusion); insufficient → 403 + problem+json with body NOT disclosing resource existence; enforced by single shared dependency across all /v1 routes. | functional / behavioral / security | DERIVED from SPEC.md lines 111–113; chose hierarchy-inclusion test, no-existence-leak body assertion, single-dependency route-introspection test. | DRAFT | 3 ACs (AC-4.1..AC-4.3). Cross-cuts NFR-02 (security). Owner: 03-development. |
| FR-05 | Rate limiting: per-token token bucket (capacity TASKQ_RATE_BURST, refill TASKQ_RATE_PER_SEC); exceeded → 429 + problem+json + Retry-After header; bucket state persisted in DB (consistent across workers) via single-transaction row-level locking; /healthz and /readyz exempt. | functional / behavioral / performance | DERIVED from SPEC.md lines 118–120; chose Retry-After-header presence assertion, cross-restart persistence test, row-lock single-transaction assertion. | DRAFT | 3 ACs (AC-5.1..AC-5.3). Maps to SPEC §8 #9. Owner: 03-development. |
| FR-06 | Persistence layer + transaction boundaries: all data access via `repository/`; business layer MUST NOT hold `Session`; one Session per request with explicit commit/rollback via context manager; string-concatenated SQL forbidden (ORM/parameterized only); eager-load via `selectinload`/`joinedload` (N+1 = acceptance failure); engine pool_size=TASKQ_DB_POOL_SIZE, pool_pre_ping=True. | architecture_constraints / functional | DERIVED from SPEC.md lines 124–128; chose repository-only-sqlalchemy (import-linter forbidden), one-Session-per-request context-manager test, SQL concat grep gate, constant-SQL-count assertion. | DRAFT | 5 ACs (AC-6.1..AC-6.5). Cross-cuts NFR-01 (perf) and NFR-06 (layering). Owner: 03-development. |
| FR-07 | Alembic three-step schema evolution: v1 creates tasks+api_keys; v2 adds tags+task_tags (many-to-many) + tasks.name unique index; v3 splits tasks.result_json into task_results with data migration; every step has working `downgrade`; round-trip reversibility preserves sample data bytewise; no `op.execute("DROP TABLE ...")` shortcuts. | functional / data-migration | DERIVED from SPEC.md lines 137–143; chose fresh-DB upgrade head, downgrade-base no residual, v2-additive, v3-data-migration real-SQLite-file, bytewise-roundtrip, no-drop-table-shortcut, offline-SQL-generation tests. | DRAFT | 7 ACs (AC-7.1..AC-7.7). Maps to SPEC §8 #12, #13. Cross-cuts NFR-09 (real-DB migration test). Owner: 03-development. |
| FR-08 | Async executor: `asyncio.TaskGroup` manages background tasks; graceful drain on shutdown up to TASKQ_DRAIN_TIMEOUT (overflow → `interrupted`); concurrency cap TASKQ_MAX_CONCURRENT with queueing; timeout via `asyncio.wait_for` killing subprocess via `process.kill()` + `await process.wait()`; `asyncio.CancelledError` propagates (NOT swallowed by `except Exception`). | functional / concurrency | DERIVED from SPEC.md lines 147–150; chose TaskGroup-concurrency-cap, graceful-drain-interrupted-marker, kill+wait no-orphans (psutil), CancelledError-propagates tests. | DRAFT | 4 ACs (AC-8.1..AC-8.4). Maps to SPEC §8 #25. Cross-cuts NFR-03 (error_handling) and FR-02 (subprocess lifecycle). Owner: 03-development. |
| FR-09 | Health checks + observability: GET /healthz (no auth) → 200 `{"status":"ok"}`; GET /readyz (no auth) → 200 iff DB reachable AND `alembic current` == head, else 503 with `detail` naming failed check (fail-closed on migration lag); GET /v1/metrics (admin) → task counts by status, execution latency percentiles, rate-limit rejection counts (no DB URL leak). | functional / observability | DERIVED from SPEC.md lines 156–158; chose healthz-200-no-auth, readyz-200-DB+head, readyz-503-DB-down, readyz-503-not-at-head (fail-closed), admin-metrics-no-DB-URL-leak tests. | DRAFT | 5 ACs (AC-9.1..AC-9.5). Maps to SPEC §8 #10, #11. Owner: 03-development. |
| FR-10 | RFC 7807 error contract: all non-2xx Content-Type = `application/problem+json`; body fields `type`/`title`/`status`/`detail`/`instance`/`correlation_id`; `detail` MUST NOT leak stack/SQL/path/schema; `correlation_id` echoed in `X-Correlation-Id` header AND server log; error-code mapping 422/401/403/404/409/429/503/500. | functional / error_contract | DERIVED from SPEC.md lines 164–168; chose problem+json Content-Type, six-field body presence, 500-detail no-internals scan, correlation-id header↔log consistency, full-mapping end-to-end tests. | DRAFT | 5 ACs (AC-10.1..AC-10.5). Maps to SPEC §8 #19. Cross-cuts NFR-02 and NFR-04. Owner: 03-development. |
| NFR-01 | Performance: GET /v1/tasks/{id} p95 < 30ms (10k records, ASGI); GET /v1/tasks?limit=50 p95 < 80ms; list endpoints issue constant SQL count (no N+1, asserted via SQLAlchemy event listener); measured via `pytest-benchmark`. | performance | DERIVED from SPEC.md lines 180–182; chose pytest-benchmark on seeded 10k dataset + SQLAlchemy event-listener statement counter. p95 thresholds NOT directly enforced by harness `performance` dimension (mean-only); ACs require dedicated tests. | DRAFT | 3 ACs (AC-N1.1..AC-N1.3). Maps to SPEC §8 #14, #15, §11. Owner: 04-testing. |
| NFR-02 | Security: codebase-wide ban on `shell=True`, `eval(`, `exec(` (grep 0 hits); no f-string / `%` / `+` SQL composition (grep + review); API keys hashed + `hmac.compare_digest` (FR-03); 403 responses do not leak existence (FR-04); error bodies contain no stack/SQL/path (FR-10); CORS default-deny with TASKQ_CORS_ORIGINS allowlist; `bandit -r 03-development/src/` 0 HIGH 0 MEDIUM. | security | DERIVED from SPEC.md lines 188–194; chose grep gates (shell/eval/exec + SQL concat), bandit CI gate, CORS default-deny test, 403-no-existence-disclosure test, 500-no-internals test. Grep ACs are NOT subsumed by bandit score. | DRAFT | 6 ACs (AC-N2.1..AC-N2.6). Maps to SPEC §8 #16, #17, #23. Cross-cuts FR-03/04/10. Owner: 04-testing + 06-quality. |
| NFR-03 | Error handling, transaction, async correctness: explicit transaction boundary per request (commit/rollback via context manager, FR-06); no bare `except:` and no `except Exception: pass`; `asyncio.CancelledError` MUST re-raise; DB connection failure → /readyz 503 with explicit detail (no infinite silent retry); task timeout terminates child process (FR-08); migration failure rolls back (FR-07). | error_handling / reliability | DERIVED from SPEC.md lines 200–204; chose AST scan (no bare except, no swallow pass), CancelledError-propagation unit test, readyz-DB-down no-infinite-retry test, failed-migration rollback test. ast-error-handling dimension does NOT detect CancelledError-swallowing specifically; AC-N3.2 requires runtime test. | DRAFT | 4 ACs (AC-N3.1..AC-N3.4). Maps to SPEC §8 #10. Cross-cuts FR-06/07/08. Owner: 04-testing. |
| NFR-04 | Sensitive-data redaction: regex `(sk-[A-Za-z0-9_-]{8,}|token=\S+|Bearer\s+\S+|postgres(ql)?://[^\s]+)` replaced with `[REDACTED]` in `stdout_tail` / `stderr_tail` / logs / error bodies; DB connection string (with password) NEVER appears in logs / error messages / /v1/metrics; API key plaintext only printed once at `key create`. | security / redaction | DERIVED from SPEC.md lines 210–211; chose runtime fixture logging all four patterns, log/metrics scan asserting no password fragment. bandit/semgrep cannot detect runtime redaction; ACs are runtime-only. | DRAFT | 2 ACs (AC-N4.1..AC-N4.2). Maps to SPEC §8 #20. Cross-cuts FR-03/09. Owner: 04-testing. |
| NFR-05 | Documentation coverage: 100% docstring coverage on public def/class with `[FR-XX]` or `[NFR-XX]` tag; each /v1 endpoint has `summary` + `description` in OpenAPI (`/openapi.json`). | documentation | DERIVED from SPEC.md lines 217–218; chose AST-docstrings scan with regex for FR/NFR tags + OpenAPI schema assertion via httpx GET /openapi.json. ast-docstrings dimension does NOT enforce tag presence; AC-N5.1 requires dedicated test. | DRAFT | 2 ACs (AC-N5.1..AC-N5.2). Owner: 03-development + 04-testing. |
| NFR-06 | Architecture layering contract: `.importlinter` declares `api > service > repository > models`; `config` and `errors` independent; additional forbidden contract: any layer other than `repository` MUST NOT import `sqlalchemy`; `lint-imports` exits 0; no bypassing via delete / blanket `ignore_imports` / contract downgrade. | architecture_constraints | DERIVED from SPEC.md lines 224–232; chose importlinter-config-present, lint-imports-exit-0, sqlalchemy-in-service-or-api-blocked, no-bypass tests. | DRAFT | 4 ACs (AC-N6.1..AC-N6.4). Maps to SPEC §8 #21. Cross-cuts FR-06. Owner: 02-architecture + 04-testing. |
| NFR-07 | Dependency + license compliance: all runtime deps pinned `==` in `requirements.txt`; transitive deps fully locked in `requirements.lock`; allowed licenses {MIT, BSD-2-Clause, BSD-3-Clause, Apache-2.0, PSF}; scan full dependency tree via `pip-licenses --format=json --with-system`; SBOM at `08-config/SBOM.json` with `name`/`version`/`license`/`direct|transitive`. | license_compliance | DERIVED from SPEC.md lines 237–240; chose requirements-pinned, pip-licenses-full-tree-allowlist, lock-file-completeness, SBOM-schema tests. license_compliance dimension uses scancode over src/ (not pip-licenses); ACs require dedicated tests. | DRAFT | 4 ACs (AC-N7.1..AC-N7.4). Maps to SPEC §8 #22. Owner: 04-testing + 08-config. |
| NFR-08 | Mutation testing: `.methodology/harness_config.json` sets `features.mutation_testing: true`; mutation score ≥ 70; scope limited to `service/` + `repository/` with rationale recorded; `crg_cohesion_healthy` MUST NOT be lowered. | mutation_testing | DERIVED from SPEC.md lines 245–246; chose `harness_cli.py mutation-test-score` exit + score ≥ 70 assertion, `harness_config.json` field assertions. | DRAFT | 3 ACs (AC-N8.1..AC-N8.3). Maps to SPEC §8 #24, §10. Owner: 04-testing + 06-quality. |
| NFR-09 | Verification authenticity (zero-skip iron rule): no `pytest.skip` / `skipif` / `xfail` / unasserted stub; `pytest 03-development/tests -q` skipped == 0; zero test functions with zero asserts; no test exclusion via `--ignore` / `-k` / `--deselect` / `collect_ignore` / removed `testpaths`; FR-07 migration tested on real SQLite file (not in-memory mock); TRACEABILITY_MATRIX VERIFIED only after test passes. | test_assertion_quality | DERIVED from SPEC.md lines 253–257; chose pytest-summary-line, ast-assertions scan, pytest-config scan for forbidden exclusions, real-file migration test, traceability-audit-log tests. | DRAFT | 5 ACs (AC-N9.1..AC-N9.5). Maps to SPEC §8 #1. Owner: 04-testing + 05-verification. |
| NFR-10 | Integration coverage: `03-development/tests/integration/` line coverage ≥ 80%; integration tests use `httpx.AsyncClient(transport=ASGITransport(app))` (MUST NOT call handler functions directly); covers full CRUD chain + each status code (401/403/404/409/422/429/503) + migration round-trip + rate-limit trigger/recovery + graceful drain. | integration_coverage | DERIVED from SPEC.md lines 262–264; chose pytest-cov integration ≥ 80%, ASGITransport-only scan, scenario-coverage checklist test. | DRAFT | 3 ACs (AC-N10.1..AC-N10.3). Maps to SPEC §8 #3. Owner: 04-testing. |
| NFR-11 | Readability: project MI (LLOC-weighted) ≥ 80; per-function CC ≤ 10; single file ≤ 400 lines; single directory ≤ 15 files; each API handler ≤ 40 lines (business logic MUST descend to `service/`). | readability | DERIVED from SPEC.md lines 269–271; chose `radon mi` aggregate, `radon cc` per function, line-count assertions, handler-LOC assertions. | DRAFT | 4 ACs (AC-N11.1..AC-N11.4). Maps to SPEC §11. Owner: 04-testing + 06-quality. |
| NFR-12 | System verification target: `Makefile` `verify-system` target chains `alembic upgrade head` → full test suite → service startup + `/healthz`+`/readyz` smoke → `alembic downgrade base` → `alembic upgrade head`; must exit 0 and print `verify-system: PASS` on stdout. | execute_verification_target | DERIVED from SPEC.md lines 277–281; chose makefile-verify-system-chains-steps, make-verify-system-exit-0-and-passes-message tests. | DRAFT | 2 ACs (AC-N12.1..AC-N12.2). Maps to SPEC §8 #27. Owner: 08-config + 05-verification. |

## Forward References

> Downstream phase docs that consume this matrix. Canonical spec source for
> every row above is the project-root `SPEC.md` (per harness SSOT).

| Phase | Deliverables |
|-------|--------------|
| 01-requirements | SPEC_TRACKING.md (this file), SRS.md, TEST_INVENTORY.yaml, TRACEABILITY_MATRIX.md |
| 02-architecture | ADR.md, SAD.md, TEST_SPEC.md |
| 04-testing | TEST_PLAN.md, TEST_RESULTS.md |
| 05-verification | BASELINE.md, VERIFICATION_REPORT.md |
| 06-quality | FINAL_SIGN_OFF.md, QUALITY_REPORT.md, RELEASE_NOTES.md |
| 07-risk | RISK_MITIGATION_PLANS.md, RISK_REGISTER.md, RISK_STATUS_REPORT.md |
| 08-config | CONFIG_RECORDS.md, RELEASE_CHECKLIST.md |

## Update log

| Date | Change | By |
|------|--------|----|
| 2026-09-05 | Initial creation (10 FR + 12 NFR transcribed from SPEC.md via 01-requirements/SRS.md) | Agent A (Round 1) |
